{
  "name": "docker-swarm-test",
  "version": "1.0.0",
  "description": "A small HTTP server for testing docker of docker swarm",
  "main": "index.js",
  "author": "Jonas",
  "license": "MIT",
  "private": true,
  "scripts": {
    "docker-full": "yarn docker-build && yarn docker-push",
    "docker-build": "docker build -t famasmaster78/docker-swarm-test .",
    "docker-push": "docker push famasmaster78/docker-swarm-test"
  },
  "dependencies": {
    "express": "^4.18.1",
    "nodemon": "^2.0.18"
  }
}
